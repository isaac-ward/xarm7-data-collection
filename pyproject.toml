[project]
name = "template"
version = "0.1.0"
description = "A template Python repository"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "scipy",
  "matplotlib",
  "tqdm",
  "hydra-core>=1.3",
]

# Tell uv that this is a package
[tool.uv]
package = true